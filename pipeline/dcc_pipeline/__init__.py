"""Authoring-side tooling for the DCC corpus.

Nothing here ships to the browser. The apps read only the compiled bundle in
`data/dist/`, and only through the horizon access layer in `packages/core`.
"""

BUNDLE_VERSION = "0.1.0"
MAX_FLOOR = 3
